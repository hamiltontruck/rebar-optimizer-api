# Adil Rebar Optimizer API

Online Google OR-Tools backend for Adil Rebar Cutting Optimizer.

## Endpoints

- `GET /` health check
- `POST /optimize` exact/feasible cutting-stock optimization grouped by rebar size

Render configuration is included in `render.yaml`.
